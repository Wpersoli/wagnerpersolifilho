{
  "name": "wagnerpersolifilho",
  "version": "2.9.7",
  "private": true,
  "description": "WAGNER.OS — portfólio Cyber Metal v2.9.7 com hero desktop full-viewport e imagem mobile 16:9 proporcional, chat Gemini, apresentação e contato preservados.",
  "type": "commonjs",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check api/chat.js && node --check api/contact.js && node --check public/js/main.js && node --check public/js/hero-effects.js && node --check public/js/hero-fidelity.js && node --check public/js/hero-motion.js",
    "test": "node --test tests/*.test.js",
    "validate": "npm run check && npm test"
  }
}
